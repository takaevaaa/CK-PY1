lambda_func_1 = lambda x1, x2: x2 + x1  # плохо присваивать lambda функции переменным
lambda_func_2 = lambda x1, x2: x2 * x1

print(lambda_func_1, lambda_func_2)  # обе функции выглядят одинаковыми по названию
