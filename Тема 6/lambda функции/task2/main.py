def task() -> str:
    list_words = [
        "Blue",
        "Purple",
        "Salmon",
        "Turquoise",
        "Cyan"
    ]

    return max(list_words, key=...)  # TODO подставить в качестве ключевой функции встроенную функцию len


print(task())
