Среди списка слов найти самое длинное слово.  
Для поиска используйте функцию max.


<div class="hint">
  У функции max(..., key=...) есть ключевая функция, в неё можно сразу подставлять функцию, например, len
</div>
